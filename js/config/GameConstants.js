export const GameConstants = {
    // Game settings
    INITIAL_LIVES: 20,
    INITIAL_GOLD: 100,
    GOLD_PER_KILL: 10,
    
    // Canvas and rendering
    CANVAS_WIDTH: 800,
    CANVAS_HEIGHT: 600,
    TILE_SIZE: 32,
    
    // Game loop
    MAX_DELTA_TIME: 0.05,
    TARGET_FPS: 60,
    
    // Debug settings
    DEBUG_MODE: false,
    SHOW_COLLIDERS: false,
    SHOW_GRID: false,
    
    // Performance
    MAX_ENTITIES: 1000,
    MAX_PARTICLES: 200,
    
    // Game balance
    WAVE_START_DELAY: 2,
    LIVES_LOST_PENALTY: 1
}; 